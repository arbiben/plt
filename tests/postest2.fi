$ tests indexing, dot operator, declare/assign, equals operator$
int x?

char func(){
new_array[0]? $this works$
int x = thing.take? $ declare and assign in the same line$
x = new_array[1]?
char y = 'a'?
if (x is y) print(T)?
}
