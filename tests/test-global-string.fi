$ this test file tests declaring global string variables and using them across multiple functionse $
$ Not in MicroC $
$ Expects output of: hello world $


str hello?
str world?

hello = "hello"?
world = "world"?

int low() {
    str low_hello?
    low_hello = getLow(hello)?
    low_hello?
    return 0?
}

int up() {
    str up_world?
    up_world = getUp(world)?
    printstring(up_world)?
    return 0?
}

int conc() {
    str concstr?
    concstr = concat(low(),up())?
    printstring(concstr)?
    return 0?
}

int main() {

    conc()?

    return 0?

}
