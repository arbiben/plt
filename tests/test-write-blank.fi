$ this test file tests writing an empty string into a non-existing text file $
$ Does not exist in MicroC $
$ Expects an output of an empty new file write_example.txt$

int main() {
    str word?
    str filename?

    word = ""? 
    filename = "write_example.txt"?

    writeFile(filename, word)?

    return 0?
}
