$ this test file tests reading the contents of a file into a string member of a struct $
$ Does not exist in MicroC $

struct boss {
    int age?
    bool interesting?
    str description?
}

int main (){
    struct boss Richard?

    Richard.description = readFile(about_Richard.txt)?
    
    printstring(Richard.description)?
    return 0?
}
