$tests passing structs into functions (should fail)$


struct person{
    int age?
    int occupation?
    arr str 5 family?
    str secret?
}

struct person o?
arr str 4 bm?

int main(){
    struct person ori?
    struct person ggg?
    ori.age = 5?
    bm[0] = "hello!"?
    ggg = getOlder(ori)?
    print(ggg.age)?
    printstring(ggg.secret)?
    printstring(ggg.family@0)?

}


struct person getOlder(struct person om){
    o.age = 7?
    o.secret = "hi"?
    o.family[0] = "hi"?
    printstring(bm@0)?
    return o?
}

