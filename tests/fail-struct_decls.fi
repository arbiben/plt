$tests passing structs into functions (should fail)$


struct person{
    int age?
    int occupation?
    arr str 5 family?
    str secret?
}

int main(){

    struct person ori?
    struct person ggg?
    ori.age = 5?
    ggg = getOlder(ori)?
    print(ggg.age)?
    printstring(ggg.secret)?
    printstring(ggg.family@0)?

}


struct person getOlder(struct person o){
    o.age = 7?
    o.secret = "hi"?
    o.family[0] = "ori"?
    return o?
}

